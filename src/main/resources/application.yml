spring:
  thymeleaf:
    mode: HTML
  profiles:
    active: prod
  messages:
    basename: i18n/messages
  http:
    encoding:
      charset: utf-8
      force: true
      enabled: true

comment.avatar: /images/avatar.png

